# Runtime dependencies for the jobhunt VPS and local development.
# Browser binaries are installed separately with:
#   python -m playwright install --with-deps chromium
PyYAML>=6.0,<7
playwright>=1.50,<2
google-auth>=2.30,<3
google-auth-oauthlib>=1.2,<2
tzdata>=2024.1
